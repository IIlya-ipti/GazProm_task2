package ConnectedObjects;

import ConnectedObjects.UsedObject;
import javafx.scene.image.ImageView;

public class Worker extends UsedObject {
    public Worker(ImageView imageView){
        super(imageView);
        //off();
    }
}
